# Reference manifest — the keys the app expects. Values here are placeholders
# and are ignored by env-drift; only the key names matter.
DATABASE_URL=postgres://localhost/myapp
API_KEY=your-key-here
STRIPE_SECRET=sk_test_xxxxxxxx
REDIS_URL=redis://localhost:6379
LOG_LEVEL=info
